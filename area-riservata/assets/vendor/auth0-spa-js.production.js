/*! Auth0 SPA JS 2.0.3 placeholder (offline vendored copy required) */
(function(){
  const msg = "Auth0 SPA SDK vendor file non disponibile in questo ambiente (download bloccato). Sostituisci area-riservata/assets/vendor/auth0-spa-js.production.js con il bundle ufficiale v2.0.3.";
  console.error(msg);
  window.__AUTH0_SDK_PLACEHOLDER__ = true;
  window.createAuth0Client = function(){
    throw new Error(msg);
  };
})();
